{
  "name": "story-stream",
  "version": "0.0.0",
  "description": "Writing stories is hard for programmers.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Verworn <evan@verworn.net>",
  "license": "MIT",
  "dependencies": {
    "relaxed-json": "^0.2.7",
    "minimist": "^1.1.0"
  },
  "devDependencies": {
    "should": "^4.1.0"
  }
}
